//! Panel rendering for the chat module.

/// Dashboard panel: always-on room list, server status, bridges, search.
pub(crate) mod dashboard;
/// Room panel: per-room message display with YAML context output.
pub(crate) mod room;
